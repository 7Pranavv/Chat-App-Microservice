spring.application.name=chat-service
server.port=8083

# Internal service URLs (could be replaced with service-discovery URLs in production)
services.room-service.url=http://localhost:8081
services.message-service.url=http://localhost:8082

management.endpoints.web.exposure.include=health,info
